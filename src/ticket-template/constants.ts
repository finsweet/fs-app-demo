import { SELECTORS_KEY } from '$utils/constants';

export const SELECTORS = {
  ticketSkeleton: `[${SELECTORS_KEY}="ticket-skeleton"]`,
  ticketComponent: `[${SELECTORS_KEY}="ticket-component"]`,
  ticketTitle: `[${SELECTORS_KEY}="ticket-title"]`,
  ticketDescription: `[${SELECTORS_KEY}="ticket-description"]`,
};
