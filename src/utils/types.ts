export type UserData = {
  name: string;
  email: string;
};

export type EventData = {
  title: string;
  location: string;
  color: string;
};

export type TicketData = {
  id: string;
  date: string;
  title: string;
  description: string;
  location: string;
};
